export enum userRol {
    DOCENTE = 0,
    COORDINADOR = 1,
    DECANO = 2,
};

export enum docenteType {
    CATEDRA = 0,
    OCASIONAL = 1,
    PLANTA = 2,
};

export enum gender {
    MEN = 0,
    WOMEN = 1,
    OTHER = 2,
}
